import React from 'react'

function Columns(){
    // const items = []
    return (
        <>
            {/* {
                items.map( item => (
                    <React.Fragment key={item.id}>
                        <h>Title</h>
                        <h>{item.title}</h>
                    </React.Fragment>
                ))
            } */}
            <td>Name</td>
            <td>Saksham</td>
        </>
    )
}

export default Columns 